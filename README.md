# pychorewars

A simple task manager with colorized output and JSON storage.

## Installing
1. Install python 3+
2. pip install colorama
3. cd 'the directory where you installed it(WITHOUT QUOTES)'
## Using
# Adding task
python main.py add Buy milk
python main.py add Kill the bill

# View all tasks
python main.py list

# Mark task #1 as completed
python main.py done 1

# Delete task #2
python main.py rm 2

# Clear all
python main.py clear

## Commands
- `add text` — add task
- `list` — view all
- `list --pending` — only active
- `done <id>` — mark ass completed
- `rm <id>` — delete
- `clear` — clear
